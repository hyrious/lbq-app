{
  "name": "lbq-app",
  "type": "module",
  "main": "main.ts",
  "private": true,
  "scripts": {
    "install": "node build/install.ts",
    "dev": "node build/electron.ts .",
    "app": "node build/app.ts",
    "exe": "node build/exe.ts",
    "typecheck": "tsc -p . --noEmit",
    "test": "node --test test/*.test.ts"
  }
}
